project "a-service"

// Guess dependencies could be also be a block?
dependencies {
    project "a-lib"
}

tasks {
    task "build" {
        dependency task="build" for-project-deps=true
        command r#"echo "build-a-service""#
    }
}
